只能想起对应的知识点。。。建议着重复习（不代表其他不考）

Java语言是跨平台的

Java程序的编译命令（javac 文件名.java、java 文件名）

Java参数传递的值传递

重载与重写的概念

this与super的概念

继承与多态（重点）

可以将一个子类的对象赋给超类变量

抽象类不一定要包含抽象方法，但类中包含了抽象方法，则该类必须被定义为抽象类

变量和方法的访问控制符

局部类不仅能够访问包含它们的外部类，还可以访问局部变量，但局部变量必须被声明为final

异常处理（重点）

repaint()的作用

常用的布局管理器

JCheckBox和JComboBox

JMenuBar的创建与使用

使用JOptionPane对象所得到的对话框都是模态对话框